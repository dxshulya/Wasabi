<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Wasabi" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blueLight</item>
        <item name="colorPrimaryVariant">@color/blueDark</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/green</item>
        <item name="colorSecondaryVariant">@color/red</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="fontFamily">@font/comfortaa</item>
        <item name="android:navigationBarColor">@color/blueDark</item>
    </style>

    <style name="Theme.Wasabi.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Wasabi.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Wasabi.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MyThemeSwitcher" parent="Widget.Material3.CompoundButton.Switch">
        <item name="trackTint">@color/theme_selector_track</item>
        <item name="android:thumb">@drawable/theme_selector</item>
        <item name="thumbTint">@color/theme_selector_thumb</item>
    </style>
</resources>